# HubSpot OAuth Scopes (Phase 1)

- `crm.objects.contacts.read`: required for contact sync reads.
- `crm.objects.contacts.write`: required for create/update sync writes.

Future phases may add property scopes if custom property creation is automated.
